import React from "react";
import $ from "jquery";

function GradPhoto() {

    if ($(window).width() < 960) {
        return (
            <div>

            </div>

        );
    }
    else {
        return (
            <div>

            </div>

        );
    }


} export default GradPhoto;
